{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "main",
	"description": "Allow the CMS page to keep the native window title in sync with its HTML title.",
	"windows": ["main"],
	"permissions": ["core:window:allow-set-title"]
}
