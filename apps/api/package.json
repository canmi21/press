{
	"name": "@canmi/api",
	"private": true,
	"type": "module",
	"scripts": {
		"db:generate": "drizzle-kit generate",
		"slugs": "node scripts/slugs.ts",
		"db:migrate:local": "wrangler d1 migrations apply api --local --env dev",
		"db:migrate:remote": "wrangler d1 migrations apply api --remote --env=\"\"",
		"types": "wrangler types --env=\"\" --include-runtime=false",
		"dev": "pnpm run slugs && pnpm run db:migrate:local && wrangler dev --env dev",
		"deploy": "pnpm run slugs && pnpm run db:migrate:remote && wrangler deploy --env=\"\"",
		"build": "pnpm run slugs && pnpm run db:migrate:remote && wrangler deploy --dry-run --outdir=dist --env=\"\""
	},
	"dependencies": {
		"@canmi/robots": "workspace:*",
		"@canmi/store": "workspace:*",
		"@canmi/urls": "workspace:*",
		"@sentry/cloudflare": "^10.73.0",
		"drizzle-orm": "^0.45.2",
		"hono": "^4.13.7"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^5.20260910.1",
		"@types/node": "^26.5.1",
		"drizzle-kit": "^0.31.10",
		"miniflare": "5.20260908.0-alpha",
		"wrangler": "^4.130.0"
	}
}
