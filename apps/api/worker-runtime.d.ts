/// <reference types="@cloudflare/workers-types" />

// Wrangler generates binding names in worker-configuration.d.ts. Runtime APIs come from the
// separately pinned package so regenerating bindings does not rewrite thousands of unrelated lines.
