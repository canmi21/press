/**
 * Re-exported from `@canmi/units`, which is where these live now that the CMS animates lengths
 * too. The path stays because ten components import it and the rule in spec/styling.md names it.
 */
export {
	DEFAULT_PIXELS_PER_REM,
	remFromDefaultPixels,
	remFromMeasuredPixels,
} from '@canmi/units';
