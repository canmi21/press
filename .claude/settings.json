{
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "^Bash$",
				"hooks": [
					{
						"type": "command",
						"command": "d=$(jj workspace root); while [ ! -f \"$d/hooks/run.py\" ] && [ \"$d\" != / ]; do d=$(dirname \"$d\"); done; /usr/bin/python3 \"$d/hooks/run.py\"",
						"timeout": 120,
						"statusMessage": "Applying repository policy"
					}
				]
			}
		],
		"PostToolUse": [
			{
				"matcher": "^Bash$",
				"hooks": [
					{
						"type": "command",
						"command": "d=$(jj workspace root); while [ ! -f \"$d/hooks/run.py\" ] && [ \"$d\" != / ]; do d=$(dirname \"$d\"); done; /usr/bin/python3 \"$d/hooks/run.py\"",
						"timeout": 30,
						"statusMessage": "Applying repository policy"
					}
				]
			}
		],
		"Stop": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "d=$(jj workspace root); while [ ! -f \"$d/hooks/run.py\" ] && [ \"$d\" != / ]; do d=$(dirname \"$d\"); done; /usr/bin/python3 \"$d/hooks/run.py\"",
						"timeout": 30,
						"statusMessage": "Applying repository policy"
					}
				]
			}
		]
	}
}
